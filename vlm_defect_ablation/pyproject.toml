[project]
name = "vlm-defect-ablation"
version = "0.1.0"
description = "Context-isolated VLM ablation for seven-slice CT stacks"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
